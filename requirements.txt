# RoboGround 依赖清单
# ============================================================
# ⚠️ 重要：不要在本文件里写 torch！
#    本机 `lxr` 环境的 torch 是 cu124 专用构建（2.5.1），
#    `pip install torch` 会用 CPU 版覆盖它，导致 GPU 不可用。
#    详见 setup_env.md。
# ============================================================

# ---- 核心（lxr 环境已装，此处仅锁版本下限）----
numpy>=1.24
scipy>=1.10
PyYAML>=6.0
tqdm>=4.65
Pillow>=10.0
matplotlib>=3.7
scikit-learn>=1.3

# ---- 视觉 / 图像 ----
opencv-python>=4.8

# ---- 可选：真实感知后端（Stage 1）----
# 装了才能用 grounding_dino / sam / dinov2 / clip 后端；
# 不装也能跑（会自动退化到 stub / 几何后端，见 docs/运行手册.md）
# transformers>=4.45
# timm>=1.0
# einops>=0.7
# safetensors>=0.4

# ---- 可选：VLM 空间推理（Stage 3）----
# peft>=0.12
# accelerate>=0.34
# deepspeed>=0.14        # Windows 上编译困难，可用 ZeRO-Offload 替代方案
# qwen-vl-utils>=0.0.8

# ---- 可选：量化 / 导出（Stage 4）----
# onnx>=1.16
# onnxruntime>=1.19

# ---- 可选：ROS2 节点（Stage 4）----
# 本项目 ROS2 节点用「防御式导入」编写：
# 没装 rclpy 也能 import 本包并通过测试，只是不能真正跑节点。
# ROS2 建议用官方安装包（Windows: ros2-humble），不要用 pip。

# ---- 开发 ----
pytest>=7.4
pytest-cov>=4.1
